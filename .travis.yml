language: php

php:
  - 7.3

branches:
  - fix/travis

install:
  - composer self-update
  - composer install --prefer-dist --no-interaction

script:
  # Backend
  - composer test-travis

cache:
  directories:
    - vendor/

addons:
  apt:
    packages:
    - libgconf-2-4
