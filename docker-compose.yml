db:
  image: mysql:latest
  environment:
    - MYSQL_DATABASE=koel
    - MYSQL_ROOT_PASSWORD=secret

web:
  build: .
  links:
    - db:db
  ports:
    - 5000:80
  volumes:
    - ~/Music:/music
  command: apache2-foreground